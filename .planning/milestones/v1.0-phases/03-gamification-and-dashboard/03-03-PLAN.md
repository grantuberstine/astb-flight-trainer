---
phase: 03-gamification-and-dashboard
plan: 03
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - src/data/missions.ts
  - src/components/dashboard/MissionCard.tsx
  - src/pages/MissionsPage.tsx
  - src/pages/ChallengePage.tsx
  - src/App.tsx
autonomous: true
requirements: [GAME-06, GAME-07]

must_haves:
  truths:
    - "User can browse available missions and see their objectives"
    - "User can start a mission and track objective progress"
    - "Completed missions award XP and optionally unlock a badge"
    - "User can take timed challenges (10 questions, tight time limit) per section"
    - "Personal best scores are tracked and displayed for each section challenge"
    - "Beating a personal best is clearly indicated on challenge completion"
  artifacts:
    - path: "src/data/missions.ts"
      provides: "6-8 mission definitions with objectives"
      exports: ["MISSIONS"]
    - path: "src/components/dashboard/MissionCard.tsx"
      provides: "Mission card with objective checklist and progress"
    - path: "src/pages/MissionsPage.tsx"
      provides: "Mission browser with active/available/completed sections"
    - path: "src/pages/ChallengePage.tsx"
      provides: "Challenge mode section picker with personal best display and quiz launch"
    - path: "src/App.tsx"
      provides: "New routes for /missions and /challenge"
  key_links:
    - from: "src/data/missions.ts"
      to: "src/types/gamification.ts"
      via: "uses Mission type"
      pattern: "Mission"
    - from: "src/pages/MissionsPage.tsx"
      to: "src/stores/gamification-store.ts"
      via: "reads missionProgress, calls startMission"
      pattern: "startMission|missionProgress"
    - from: "src/pages/ChallengePage.tsx"
      to: "src/stores/gamification-store.ts"
      via: "reads challengeRecords, calls recordChallenge"
      pattern: "recordChallenge|challengeRecords"
    - from: "src/App.tsx"
      to: "src/pages/MissionsPage.tsx"
      via: "route registration"
      pattern: "/missions"
---

<objective>
Build the mission system and timed challenge mode. Missions are curated study sequences with objectives. Challenges are short timed quizzes (10 questions) with personal best tracking per section.

Purpose: Give the user structured study paths (missions) and a competitive mode (challenges) that adds replayability and motivation beyond free practice.
Output: Mission data, mission browser page, challenge page with PB tracking, and routes.
</objective>

<execution_context>
@C:/Users/grant/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/grant/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-gamification-and-dashboard/03-RESEARCH.md
@.planning/phases/03-gamification-and-dashboard/03-01-SUMMARY.md

@src/types/gamification.ts
@src/lib/constants.ts
@src/stores/gamification-store.ts
@src/components/quiz/QuizSession.tsx
@src/App.tsx
@src/pages/PracticePage.tsx

<interfaces>
<!-- From Plan 01 (must exist before this plan runs) -->

From src/types/gamification.ts:
```typescript
export interface Mission {
  id: string; name: string; description: string;
  objectives: MissionObjective[]; xpReward: number; badgeId?: string;
}
export interface MissionObjective {
  type: 'complete-questions' | 'score-threshold' | 'speed-challenge' | 'streak';
  sectionId?: SectionId; target: number; label: string;
}
export interface MissionProgress {
  missionId: string; startedAt: number;
  objectiveProgress: boolean[]; completedAt: number | null;
}
export interface ChallengeRecord {
  sectionId: SectionId; score: number; timeMs: number; date: number;
}
```

From src/stores/gamification-store.ts (extended in Plan 01):
```typescript
// State: challengeRecords: ChallengeRecord[], missionProgress: MissionProgress[]
// Actions: startMission(id), updateMissionObjective(id, idx, completed), completeMission(id), recordChallenge(record)
```

From src/lib/constants.ts:
```typescript
export const XP_VALUES = { correctAnswer: 10, perfectSection: 50, dailyGoal: 25, missionComplete: 100 } as const;
export const ASTB_SECTIONS: ASTBSection[];
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Mission definitions data and MissionCard component</name>
  <files>src/data/missions.ts, src/components/dashboard/MissionCard.tsx</files>
  <action>
**src/data/missions.ts** — Create 7 mission definitions using the Mission type. Each mission has 2-3 objectives. Missions should cover common study paths:

1. **"First Sortie"** — Introductory mission. Objectives: complete 10 questions in any section, score 60%+. XP: 50.
2. **"Math Navigator"** — MST focused. Objectives: complete 20 MST questions, score 80%+ on MST. XP: 100.
3. **"Bookworm Aviator"** — RCT focused. Objectives: complete 15 RCT questions, score 75%+ on RCT. XP: 100.
4. **"Grease Monkey"** — MCT focused. Objectives: complete 20 MCT questions, score 80%+ on MCT. XP: 100.
5. **"Flight School"** — ANIT focused. Objectives: complete 20 ANIT questions, score 80%+ on ANIT. XP: 100.
6. **"Eagle Eye"** — SAT focused. Objectives: complete 15 SAT questions, score 75%+ on SAT. XP: 100.
7. **"Cross-Training"** — Multi-section. Objectives: practice all 5 sections, score 70%+ average across all. XP: 200. Badge: 'all-sections'.

Export as `MISSIONS` array typed as `Mission[]`.

**src/components/dashboard/MissionCard.tsx** — Mission card component. Props: `mission: Mission, progress: MissionProgress | undefined, onStart: () => void`. Shows:
- Mission name and description
- Objective checklist with checkmarks for completed objectives
- Progress bar (completed objectives / total)
- If not started: "Start Mission" button (gold-500)
- If in progress: "In Progress" badge with progress percentage
- If completed: "Completed" badge (green) with completion date and XP earned
- Card style: bg-navy-800 rounded-lg p-5, border-l-4 with gold for active, green for complete, navy-600 for available
  </action>
  <verify>
    <automated>cd "C:/Users/grant/My Drive/Claude Projects/astb-flight-trainer" && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>
    - 7 mission definitions exported from data/missions.ts
    - MissionCard renders mission with objective checklist and status
    - TypeScript compiles clean
  </done>
</task>

<task type="auto">
  <name>Task 2: Missions page, Challenge page, and route wiring</name>
  <files>src/pages/MissionsPage.tsx, src/pages/ChallengePage.tsx, src/App.tsx</files>
  <action>
**src/pages/MissionsPage.tsx** — Mission browser page. Reads missionProgress from gamification store. Groups missions into three sections:
1. **Active Missions** — started but not completed (filter missionProgress for completedAt === null)
2. **Available Missions** — not yet started (no MissionProgress entry)
3. **Completed Missions** — completedAt !== null

Each section header with count. Renders MissionCard for each mission. "Start Mission" button calls `startMission(mission.id)` on the store. Include header with Crosshair icon and "Missions" title, matching the app's existing page header pattern (see DashboardPage, PracticePage).

**src/pages/ChallengePage.tsx** — Timed challenge section picker with personal best tracking. Layout similar to PracticePage but focused on challenges.

Shows each ASTB section as a card with:
- Section name and icon
- Personal best score and time (from challengeRecords in store, or "No record yet")
- "Start Challenge" button

Challenge config: 10 questions, 120 seconds (2 minutes) time limit for all sections. This is intentionally tighter than real ASTB timing to create pressure.

When user clicks "Start Challenge", navigate to `/quiz/{sectionId}?mode=challenge`. The QuizSession component already supports `mode` prop — add 'challenge' to the mode union type in QuizSession.tsx (or handle via the page that renders QuizSession).

Actually, to keep QuizSession changes minimal (Plan 02 is modifying it), use a simpler approach: navigate to a challenge quiz route that renders QuizSession with `mode="timed"`, `questionCount={10}`, and a custom `timeLimitSec` override. On completion (`onComplete` callback), call `recordChallenge` on the store with the score and time, and check if it beat the personal best.

Add an `onComplete` handler that:
1. Calculates score percentage from answers
2. Calculates total time from answers' timeMs
3. Calls `recordChallenge({ sectionId, score, timeMs, date: Date.now() })`
4. Compares against previous best — if beaten, show "New Personal Best!" in the completion UI
5. Awards XP for the challenge completion

Use a local state to track whether PB was beaten for display in the completion view.

**src/App.tsx** — Add two new routes to the hash router:
- `/missions` → MissionsPage
- `/challenge` → ChallengePage
- `/challenge/:sectionId` → ChallengePage with section pre-selected (or a separate ChallengeQuizPage if cleaner)

Also add navigation links to the sidebar/nav. Check existing nav component (likely in `src/components/layout/`) and add "Missions" (Crosshair icon) and "Challenges" (Timer icon) nav items.
  </action>
  <verify>
    <automated>cd "C:/Users/grant/My Drive/Claude Projects/astb-flight-trainer" && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>
    - Missions page shows available/active/completed missions with MissionCards
    - Challenge page shows all 5 sections with personal best records
    - Starting a challenge launches a 10-question timed quiz
    - Challenge completion records score and detects personal bests
    - Routes registered and accessible from navigation
    - Build passes clean
  </done>
</task>

</tasks>

<verification>
- `npm run build` passes clean
- /missions route renders mission browser with 7 missions
- /challenge route renders section picker with PB display
- Starting a mission creates MissionProgress in store
- Challenge completion records ChallengeRecord in store
- Navigation includes links to Missions and Challenges
</verification>

<success_criteria>
- 7 missions with meaningful study-path objectives
- Mission browser shows active/available/completed grouping
- Challenge mode runs 10-question timed quizzes per section
- Personal bests tracked per section and displayed prominently
- New PB detection shown clearly on challenge completion
- Routes wired and navigable from app sidebar
</success_criteria>

<output>
After completion, create `.planning/phases/03-gamification-and-dashboard/03-03-SUMMARY.md`
</output>
