---
phase: 01-app-shell-and-data-layer
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/App.tsx
  - src/components/layout/AppLayout.tsx
  - src/components/layout/Navbar.tsx
  - src/pages/DashboardPage.tsx
  - src/pages/PracticePage.tsx
  - src/pages/ProgressPage.tsx
  - src/pages/SettingsPage.tsx
  - src/lib/export-import.ts
autonomous: false
requirements:
  - DATA-02
  - DATA-03

must_haves:
  truths:
    - "User can navigate between Dashboard, Practice, Progress, and Settings pages"
    - "User can export all progress as a JSON file download"
    - "User can import a JSON backup file and have state restored"
    - "App runs entirely client-side with hash-based routing (no server config needed)"
    - "Navigation shows active route indicator"
  artifacts:
    - path: "src/App.tsx"
      provides: "Hash router with nested routes"
      contains: "createHashRouter"
    - path: "src/components/layout/AppLayout.tsx"
      provides: "Root layout with navbar and content outlet"
      exports: ["AppLayout"]
    - path: "src/components/layout/Navbar.tsx"
      provides: "Navigation bar with route links and active state"
      exports: ["Navbar"]
    - path: "src/pages/SettingsPage.tsx"
      provides: "Settings page with export/import buttons"
      contains: "exportAllData"
    - path: "src/lib/export-import.ts"
      provides: "JSON export/import utilities for all persisted stores"
      exports: ["exportAllData", "importAllData"]
  key_links:
    - from: "src/App.tsx"
      to: "src/components/layout/AppLayout.tsx"
      via: "router config element"
      pattern: "element.*AppLayout"
    - from: "src/lib/export-import.ts"
      to: "src/lib/storage.ts"
      via: "PERSISTED_STORE_KEYS import"
      pattern: "PERSISTED_STORE_KEYS"
    - from: "src/pages/SettingsPage.tsx"
      to: "src/lib/export-import.ts"
      via: "export/import function calls"
      pattern: "exportAllData|importAllData"
    - from: "src/App.tsx"
      to: "src/components/layout/HydrationGate.tsx"
      via: "wraps RouterProvider"
      pattern: "HydrationGate"
---

<objective>
Build the app shell with hash-based routing, navigation layout, placeholder pages, and the JSON export/import system for data backup.

Purpose: Users need to navigate the app and protect their study data with export/import. Hash routing ensures the app works on any static host (GitHub Pages, Vercel, Netlify) without server configuration.
Output: A navigable app shell with working export/import on the Settings page.
</objective>

<execution_context>
@C:/Users/grant/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/grant/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-app-shell-and-data-layer/01-RESEARCH.md
@.planning/phases/01-app-shell-and-data-layer/01-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 - types and exports the executor needs -->

From src/lib/storage.ts:
```typescript
export const indexedDBStorage: PersistStorage<unknown>;
export const PERSISTED_STORE_KEYS: readonly ['astb-progress', 'astb-gamification', 'astb-settings'];
```

From src/stores/progress-store.ts:
```typescript
export const useProgressStore: UseBoundStore<StoreApi<ProgressState>>;
// has .persist.hasHydrated()
```

From src/stores/gamification-store.ts:
```typescript
export const useGamificationStore: UseBoundStore<StoreApi<GamificationState>>;
```

From src/stores/settings-store.ts:
```typescript
export const useSettingsStore: UseBoundStore<StoreApi<SettingsState>>;
```

From src/components/layout/HydrationGate.tsx:
```typescript
export function HydrationGate({ children }: { children: React.ReactNode }): JSX.Element;
```

From src/lib/constants.ts:
```typescript
export const ASTB_SECTIONS: Array<{ id: SectionId; name: string; description: string; icon: string; color: string }>;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create hash router, app layout, navbar, and placeholder pages</name>
  <files>src/App.tsx, src/components/layout/AppLayout.tsx, src/components/layout/Navbar.tsx, src/pages/DashboardPage.tsx, src/pages/PracticePage.tsx, src/pages/ProgressPage.tsx, src/pages/SettingsPage.tsx</files>
  <action>
    1. Create src/App.tsx using createHashRouter (not createBrowserRouter -- GitHub Pages compatibility):
       - Import from 'react-router' (not 'react-router-dom' -- v7 merged packages)
       - Root route '/' uses AppLayout as element
       - Children routes: index (DashboardPage), 'practice' (PracticePage), 'progress' (ProgressPage), 'settings' (SettingsPage)
       - Wrap RouterProvider with HydrationGate from plan 01

    2. Create src/components/layout/Navbar.tsx:
       - Horizontal top navbar with navy-800 background
       - App title "ASTB Flight Trainer" on the left with a Plane icon from lucide-react
       - Nav links: Dashboard (LayoutDashboard icon), Practice (Target icon), Progress (TrendingUp icon), Settings (Settings icon)
       - Use NavLink from react-router with active state styling (gold-400 text + bottom border when active, navy-300 text when inactive)
       - Responsive: on mobile, icons only; on md+, icons with labels

    3. Create src/components/layout/AppLayout.tsx:
       - Renders Navbar at top
       - Renders Outlet below for child routes
       - Full height layout: min-h-screen bg-navy-900 text-white
       - Content area has max-w-7xl mx-auto px-4 py-6

    4. Create placeholder pages (each as a simple component with the section title and brief description):
       - DashboardPage.tsx: "Mission Control" heading, "Your cockpit dashboard -- coming soon" text, styled with navy-800 card on navy-900 background
       - PracticePage.tsx: "Practice Missions" heading, list the 5 ASTB sections from ASTB_SECTIONS constant as cards (use section name, description, icon, and color), each card is non-interactive for now
       - ProgressPage.tsx: "Flight Log" heading, "Track your training progress -- coming soon" text
       - SettingsPage.tsx: "Settings" heading -- this page will get export/import in Task 2

    5. Update src/main.tsx to render App component (replace Vite default content). Ensure index.css is imported.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>App loads at localhost with hash routing, navbar shows 4 navigation links with active state, clicking each link renders the correct page, Practice page shows 5 ASTB section cards</done>
</task>

<task type="auto">
  <name>Task 2: Build export/import utilities and wire into Settings page</name>
  <files>src/lib/export-import.ts, src/pages/SettingsPage.tsx</files>
  <action>
    1. Create src/lib/export-import.ts:
       - Import get, set from idb-keyval
       - Import PERSISTED_STORE_KEYS from ./storage
       - exportAllData(): iterate over PERSISTED_STORE_KEYS, read each from IndexedDB via get(), build a data object with a meta field (version: 1, exportedAt: ISO timestamp, app: 'astb-flight-trainer'), create Blob with JSON.stringify(data, null, 2), trigger download as 'astb-backup-YYYY-MM-DD.json' using createObjectURL + anchor click pattern, then revokeObjectURL
       - importAllData(file: File): read file via file.text(), JSON.parse, validate meta.app === 'astb-flight-trainer' (throw Error if not), iterate PERSISTED_STORE_KEYS and set() each into IndexedDB, then window.location.reload() to rehydrate stores
       - Export both functions

    2. Update src/pages/SettingsPage.tsx:
       - "Settings" heading
       - "Data Management" section with a navy-800 card containing:
         - "Download Backup" button (gold-500 bg, navy-900 text) that calls exportAllData()
         - "Restore Backup" section with a file input (accept=".json") that calls importAllData() on file selection
         - Add a confirmation dialog before import: "This will replace all your current data. Continue?"
         - Show success/error feedback after import attempt (simple state-based message)
       - "About" section showing app name and version
       - "Danger Zone" section with a "Reset All Progress" button (danger color) that clears all stores and reloads -- add confirmation dialog
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>Export button triggers JSON file download with all store data and metadata, import reads a JSON file and restores data to IndexedDB then reloads, Settings page shows data management controls with confirmation dialogs</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify complete app shell with navigation, persistence, and export/import</name>
  <files>N/A</files>
  <action>
    Human verifies the complete Phase 1 output. What was built: App shell with hash routing, 4 navigable pages, IndexedDB persistence via Zustand stores, and JSON export/import on the Settings page.

    Steps to verify:
    1. Run `npm run dev` and open the app in browser
    2. Verify navigation: click Dashboard, Practice, Progress, Settings -- each shows correct page content
    3. Verify Practice page shows 5 ASTB section cards (MST, RCT, MCT, ANIT, SAT)
    4. Verify active nav link is highlighted in gold
    5. Go to Settings, click "Download Backup" -- a JSON file should download
    6. Open the downloaded JSON file -- it should contain store keys and metadata
    7. Open browser DevTools > Application > IndexedDB -- verify 'keyval-store' database exists with store entries
    8. Close the browser tab entirely, reopen the app -- data should persist (no flash of empty state thanks to HydrationGate)
    9. Verify the URL uses hash format: http://localhost:5173/#/ , /#/practice , /#/settings
    10. Verify app has dark navy background with gold accents (aviation theme)
  </action>
  <verify>Human approval</verify>
  <done>User types "approved" confirming all 10 verification steps pass</done>
</task>

</tasks>

<verification>
1. `npm run build` produces dist/ folder with static files only (DATA-03)
2. Hash-based URLs work: /#/, /#/practice, /#/progress, /#/settings
3. IndexedDB contains persisted store data after any state change (DATA-01)
4. Export downloads valid JSON, import restores from JSON (DATA-02)
5. No external API calls in Network tab (DATA-03)
</verification>

<success_criteria>
- User can navigate between 4 pages via navbar with active state indication
- Hash routing works (URLs contain #)
- Export downloads a JSON file with all store data
- Import restores data from a valid JSON file
- App is fully static -- no backend, no API calls
- Aviation theme visible (dark navy background, gold accents)
</success_criteria>

<output>
After completion, create `.planning/phases/01-app-shell-and-data-layer/01-02-SUMMARY.md`
</output>
