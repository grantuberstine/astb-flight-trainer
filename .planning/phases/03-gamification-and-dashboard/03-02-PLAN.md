---
phase: 03-gamification-and-dashboard
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - src/components/dashboard/DashboardStats.tsx
  - src/components/dashboard/SectionScoreCard.tsx
  - src/components/dashboard/RankProgress.tsx
  - src/components/dashboard/StreakTracker.tsx
  - src/components/dashboard/BadgeGrid.tsx
  - src/components/dashboard/RecentActivity.tsx
  - src/components/dashboard/ProgressRing.tsx
  - src/components/gamification/XPNotification.tsx
  - src/pages/DashboardPage.tsx
  - src/pages/ProgressPage.tsx
  - src/components/quiz/QuizSession.tsx
  - src/components/quiz/SessionSummary.tsx
autonomous: true
requirements: [GAME-01, GAME-02, GAME-03, GAME-04, GAME-05]

must_haves:
  truths:
    - "Dashboard shows XP total, current rank with progress bar, and streak count"
    - "Each ASTB section has a score card showing correct/total and percentage ring"
    - "Badge grid shows earned badges highlighted and locked badges grayed out"
    - "Streak tracker shows current streak, longest streak, and freeze availability"
    - "XP notification appears when earning XP during quiz sessions"
    - "Quiz session awards XP on correct answers and calls streak/badge evaluation on completion"
  artifacts:
    - path: "src/pages/DashboardPage.tsx"
      provides: "Cockpit dashboard composing all widget components"
      min_lines: 30
    - path: "src/components/dashboard/DashboardStats.tsx"
      provides: "XP, rank, streak summary cards"
    - path: "src/components/dashboard/SectionScoreCard.tsx"
      provides: "Per-section score ring"
    - path: "src/components/dashboard/RankProgress.tsx"
      provides: "Rank badge with progress bar to next rank"
    - path: "src/components/dashboard/StreakTracker.tsx"
      provides: "Streak display with freeze indicator"
    - path: "src/components/dashboard/BadgeGrid.tsx"
      provides: "Earned/locked badge grid"
    - path: "src/components/gamification/XPNotification.tsx"
      provides: "Floating XP toast notification"
    - path: "src/components/quiz/QuizSession.tsx"
      provides: "XP award integration in handleAnswer and session complete"
  key_links:
    - from: "src/pages/DashboardPage.tsx"
      to: "src/stores/gamification-store.ts"
      via: "useGamificationStore selectors"
      pattern: "useGamificationStore"
    - from: "src/pages/DashboardPage.tsx"
      to: "src/stores/progress-store.ts"
      via: "useProgressStore selectors"
      pattern: "useProgressStore"
    - from: "src/components/quiz/QuizSession.tsx"
      to: "src/stores/gamification-store.ts"
      via: "addXP, updateStreak, earnBadges calls"
      pattern: "addXP|updateStreak|earnBadges"
    - from: "src/components/dashboard/RankProgress.tsx"
      to: "src/lib/xp.ts"
      via: "getRankProgress"
      pattern: "getRankProgress"
    - from: "src/components/dashboard/BadgeGrid.tsx"
      to: "src/data/badges.ts"
      via: "BADGE_DEFINITIONS for full badge list"
      pattern: "BADGE_DEFINITIONS"
---

<objective>
Build the cockpit dashboard UI and wire XP/badge/streak into the quiz flow. Dashboard displays all gamification state (XP, rank, streaks, scores, badges). QuizSession awards XP on correct answers and evaluates badges on session completion.

Purpose: This is the core gamification experience — making study feel like a game with visible progress and rewards.
Output: Fully functional dashboard page, progress page, XP notification, and gamified quiz flow.
</objective>

<execution_context>
@C:/Users/grant/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/grant/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-gamification-and-dashboard/03-RESEARCH.md
@.planning/phases/03-gamification-and-dashboard/03-01-SUMMARY.md

@src/pages/DashboardPage.tsx
@src/pages/ProgressPage.tsx
@src/components/quiz/QuizSession.tsx
@src/components/quiz/SessionSummary.tsx
@src/stores/gamification-store.ts
@src/stores/progress-store.ts
@src/lib/constants.ts
@src/lib/xp.ts
@src/lib/badges.ts
@src/data/badges.ts
@src/types/gamification.ts
@src/types/progress.ts
@src/index.css

<interfaces>
<!-- From Plan 01 (must exist before this plan runs) -->

From src/lib/xp.ts:
```typescript
export function getRankProgress(xp: number, currentRank: Rank): {
  currentXP: number; nextThreshold: number; progress: number; nextRank: Rank | null;
};
```

From src/lib/badges.ts:
```typescript
export function evaluateNewBadges(context: BadgeContext): string[];
```

From src/data/badges.ts:
```typescript
export const BADGE_DEFINITIONS: BadgeDefinition[];
// 20 badges, each with id, name, description, icon, category, check
```

From src/stores/gamification-store.ts (extended in Plan 01):
```typescript
// Existing: xp, rank, currentStreak, longestStreak, lastActiveDate, streakFreezeAvailable, badges
// New: challengeRecords, missionProgress, streakFreezesUsed
// New actions: earnBadges(ids), recordChallenge(record), startMission(id), updateMissionObjective(...), completeMission(id)
```

From src/types/gamification.ts (extended in Plan 01):
```typescript
export interface BadgeDefinition { id: string; name: string; description: string; icon: string; category: string; check: (ctx: BadgeContext) => boolean; }
export interface BadgeContext { xp: number; currentStreak: number; longestStreak: number; sectionScores: Record<string, SectionScore>; totalQuestionsAnswered: number; sectionsAttempted: Set<string>; hasPerfectSession: boolean; usedStreakFreeze: boolean; fastCorrectCount: number; timedTestBestScore: number; personalBestBeaten: boolean; badges: Badge[]; }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dashboard widget components and pages</name>
  <files>
    src/components/dashboard/ProgressRing.tsx,
    src/components/dashboard/DashboardStats.tsx,
    src/components/dashboard/SectionScoreCard.tsx,
    src/components/dashboard/RankProgress.tsx,
    src/components/dashboard/StreakTracker.tsx,
    src/components/dashboard/BadgeGrid.tsx,
    src/components/dashboard/RecentActivity.tsx,
    src/components/gamification/XPNotification.tsx,
    src/pages/DashboardPage.tsx,
    src/pages/ProgressPage.tsx
  </files>
  <action>
Create dashboard components using aviation theme tokens (navy-800, navy-700, gold-400, gold-500, cockpit-gray). All components use named exports.

**src/components/dashboard/ProgressRing.tsx** — Reusable circular SVG progress ring. Props: `percentage: number, size?: number (default 80), strokeWidth?: number (default 6), color?: string (default 'text-gold-400'), label?: string`. Renders two concentric circles: background ring (text-navy-700) and progress ring with strokeDasharray/offset calculation. Optional center label.

**src/components/dashboard/DashboardStats.tsx** — Top-level stats bar. Props: `xp: number, rank: Rank, streak: number, questionsAnswered: number`. Renders 4 stat cards in a row (responsive: 2x2 on mobile, 4-col on lg). Each card: icon (from Lucide: Star for XP, Shield for rank, Flame for streak, Target for questions), value, label. Use bg-navy-800 rounded-lg p-4.

**src/components/dashboard/SectionScoreCard.tsx** — Per-section score display. Props: `section: ASTBSection, score: SectionScore`. Shows section icon (use dynamic Lucide icon lookup or just map the 5 known icons), section name, ProgressRing with percentage (score.correct/score.total * 100, or 0 if total is 0), and "X/Y correct" text. Card style: bg-navy-800 rounded-lg p-4.

**src/components/dashboard/RankProgress.tsx** — Current rank with progress bar to next. Props: none (reads from gamification store directly). Uses `getRankProgress(xp, rank)` from `src/lib/xp.ts`. Shows current rank name with icon, a horizontal progress bar (Tailwind bg-navy-700 with gold-500 fill), XP count, and "Next: {nextRank} at {nextThreshold} XP" text. If max rank, show "Max Rank Achieved".

**src/components/dashboard/StreakTracker.tsx** — Streak display. Props: none (reads from gamification store). Shows current streak with Flame icon, longest streak, and streak freeze availability (Shield icon, green if available, gray if not). Shows "Study today to keep your streak!" if lastActiveDate is not today.

**src/components/dashboard/BadgeGrid.tsx** — Badge grid showing all 20 badges. Props: none (reads from gamification store + BADGE_DEFINITIONS). Maps over BADGE_DEFINITIONS, checks which are earned (by matching badge.id to earned badges array). Earned: full color with earned date. Locked: grayscale/opacity-40 with lock icon overlay. Grid: 4 cols on lg, 3 on md, 2 on sm. Each badge: icon (Lucide), name, description.

**src/components/dashboard/RecentActivity.tsx** — Last 10 quiz answers. Props: `history: AnswerRecord[]`. Shows recent answers in a compact list: section badge, question ID, correct/incorrect indicator (green check / red x), time ago. If empty, show "No activity yet — start practicing!"

**src/components/gamification/XPNotification.tsx** — Floating notification. Props: `amount: number, visible: boolean`. Fixed position top-right, bg-gold-500 text-navy-900, shows "+{amount} XP". CSS transition for fade in/out (translate-y + opacity). Use `pointer-events-none` when not visible.

**src/pages/DashboardPage.tsx** — Replace placeholder. Compose all dashboard widgets. Layout:
1. DashboardStats row (xp, rank, streak, questions answered)
2. RankProgress bar
3. Section scores grid (5 SectionScoreCards)
4. StreakTracker
5. BadgeGrid
6. RecentActivity (last 10)

Read from stores using selectors: `useGamificationStore(s => s.xp)`, `useProgressStore(s => s.sectionScores)`, etc. Use ASTB_SECTIONS from constants to iterate sections.

**src/pages/ProgressPage.tsx** — Update from placeholder. Show detailed progress: section-by-section breakdown with score cards, total questions answered, time spent, and link to dashboard. Reuse SectionScoreCard components.
  </action>
  <verify>
    <automated>cd "C:/Users/grant/My Drive/Claude Projects/astb-flight-trainer" && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>
    - Dashboard shows XP, rank, streak, section scores, badges, recent activity
    - ProgressPage shows detailed section breakdown
    - XPNotification renders floating toast
    - All components use aviation theme tokens
    - Build passes clean
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire XP awards and badge evaluation into QuizSession</name>
  <files>src/components/quiz/QuizSession.tsx, src/components/quiz/SessionSummary.tsx</files>
  <action>
**src/components/quiz/QuizSession.tsx** — Integrate gamification into the existing quiz flow. Import `useGamificationStore` and `XP_VALUES` from constants.

**In handleAnswer callback** (after existing `recordAnswer` call):
- If answer is correct: call `addXP(XP_VALUES.correctAnswer)` and show XP notification
- Track XP notification state with useState: `xpNotification: { amount: number; visible: boolean }`
- Set visible=true, then setTimeout 1500ms to set visible=false
- IMPORTANT: Only award XP once per answer. The existing recordAnswer already runs — just add the XP call after it.

**On session complete** (add a new useEffect watching `state.status === 'complete'`):
- Call `updateStreak()` once
- Calculate if session was perfect (all answers correct)
- If perfect: `addXP(XP_VALUES.perfectSection)`
- Build BadgeContext from progress store + gamification store state:
  - `totalQuestionsAnswered`: questionHistory.length
  - `sectionsAttempted`: new Set of sections from questionHistory
  - `hasPerfectSession`: all answers in this session correct
  - `fastCorrectCount`: count of answers in this session that were correct AND timeSpentMs < 12000
  - Other fields from store state
- Call `evaluateNewBadges(context)` — if returns non-empty, call `earnBadges(newBadgeIds)`

**Add XPNotification component** to the JSX, positioned above the quiz content. Render `<XPNotification amount={xpNotification.amount} visible={xpNotification.visible} />`.

**src/components/quiz/SessionSummary.tsx** — Add gamification summary to the completion screen:
- Show XP earned this session (count correct * XP_VALUES.correctAnswer + perfect bonus if applicable)
- Show any newly earned badges (pass as prop or read from a session-scoped ref)
- Show rank progress update if rank changed
- Keep existing UI (score, time, action buttons) but add gamification section below the score.
  </action>
  <verify>
    <automated>cd "C:/Users/grant/My Drive/Claude Projects/astb-flight-trainer" && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>
    - Correct answers award 10 XP with floating notification
    - Perfect sessions award 50 XP bonus
    - Streak updates on session completion
    - Badges evaluated and earned on session completion
    - Session summary shows XP earned and new badges
    - No double-counting of XP (per RESEARCH.md Pitfall 1)
    - Build passes clean
  </done>
</task>

</tasks>

<verification>
- `npm run build` passes clean
- Dashboard renders with all widgets pulling live data from stores
- QuizSession awards XP on correct answers (10 XP each)
- Session completion triggers streak update and badge evaluation
- XP notification appears briefly on correct answer
- Badge grid shows all 20 badges with earned/locked states
</verification>

<success_criteria>
- Cockpit dashboard replaces placeholder with functional widgets
- XP flows from quiz answers into gamification store and displays on dashboard
- Badges auto-evaluate and earn on session completion
- Streak tracker shows current and longest streak with freeze indicator
- All 5 section score cards display with progress rings
- Aviation theme applied consistently (navy, gold, cockpit-gray)
</success_criteria>

<output>
After completion, create `.planning/phases/03-gamification-and-dashboard/03-02-SUMMARY.md`
</output>
