---
phase: 02-quiz-engine-and-content
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/data/questions/mst.ts
  - src/data/questions/rct.ts
  - src/data/lessons/mst-lessons.ts
  - src/data/lessons/rct-lessons.ts
autonomous: true
requirements:
  - CONT-01
  - CONT-02
  - CONT-06

must_haves:
  truths:
    - "MST section has 100+ questions covering arithmetic, algebra, geometry, and word problems with explanations"
    - "RCT section has 50+ passage-based questions covering main idea, inference, and vocabulary with explanations"
    - "MST lesson content teaches math concepts with 3-5 concept cards per topic"
    - "RCT lesson content teaches reading comprehension strategies with concept cards"
  artifacts:
    - path: "src/data/questions/mst.ts"
      provides: "MST question bank"
      contains: "mstQuestions"
      min_lines: 500
    - path: "src/data/questions/rct.ts"
      provides: "RCT question bank"
      contains: "rctQuestions"
      min_lines: 300
    - path: "src/data/lessons/mst-lessons.ts"
      provides: "MST lesson content"
      contains: "mstLessons"
    - path: "src/data/lessons/rct-lessons.ts"
      provides: "RCT lesson content"
      contains: "rctLessons"
  key_links:
    - from: "src/data/questions/mst.ts"
      to: "src/types/question.ts"
      via: "MultipleChoiceQuestion type"
      pattern: "MultipleChoiceQuestion"
    - from: "src/data/questions/rct.ts"
      to: "src/types/question.ts"
      via: "PassageQuestion type"
      pattern: "PassageQuestion"
---

<objective>
Author the MST and RCT question banks and lesson content. MST needs 100+ multiple-choice math questions across arithmetic, algebra, geometry, and word problems. RCT needs 50+ passage-based reading comprehension questions. Both need lesson concept cards teaching the underlying material.

Purpose: These two sections represent the text-based question types (standard multiple-choice and passage-based). Filling these banks makes Practice and Timed Test modes functional for MST and RCT.
Output: Populated question arrays and lesson data for MST and RCT sections.
</objective>

<execution_context>
@C:/Users/grant/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/grant/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-quiz-engine-and-content/02-RESEARCH.md
@.planning/phases/02-quiz-engine-and-content/02-01-SUMMARY.md

<interfaces>
<!-- Types from Plan 01 that content must conform to -->

From src/types/question.ts (after Plan 01 extensions):
```typescript
export interface MultipleChoiceQuestion extends BaseQuestion {
  type: 'multiple-choice';
  text: string;
  options: [string, string, string, string];
  correctAnswer: 0 | 1 | 2 | 3;
  explanation: string;
  optionExplanations?: [string, string, string, string];
}

export interface PassageSubQuestion {
  text: string;
  options: [string, string, string, string];
  correctAnswer: 0 | 1 | 2 | 3;
  explanation: string;
  optionExplanations?: [string, string, string, string];
}

export interface PassageQuestion extends BaseQuestion {
  type: 'passage';
  passage: string;
  questions: PassageSubQuestion[];
}
```

From src/types/lesson.ts (created in Plan 01):
```typescript
export interface ConceptCard {
  id: string;
  heading: string;
  content: string;
  svgDiagram?: React.ComponentType;
  keyTakeaway?: string;
}

export interface SectionLesson {
  sectionId: SectionId;
  title: string;
  topics: Array<{ name: string; cards: ConceptCard[] }>;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: MST question bank and lessons</name>
  <files>src/data/questions/mst.ts, src/data/lessons/mst-lessons.ts</files>
  <action>
**MST Question Bank (src/data/questions/mst.ts):**
Author 100+ MultipleChoiceQuestion items in the `mstQuestions` array. All questions must be ASTB-appropriate difficulty (military officer candidate level math -- not trivial, not graduate-level).

**Distribution across topics (aim for roughly even coverage):**
- Arithmetic (~25): fractions, decimals, percentages, ratios, order of operations, estimation
- Algebra (~25): solving equations, inequalities, systems of equations, word-to-equation translation, exponents/radicals
- Geometry (~25): area, perimeter, volume, angles, triangles (Pythagorean theorem, special triangles), circles, coordinate geometry
- Word Problems (~25): rate/time/distance, work problems, mixture problems, percent change, probability

**Quality standards (per user: "content quality over quantity -- 300 excellent questions beat 600 mediocre ones"):**
- Each question has a clear, unambiguous correct answer
- Distractors (wrong options) should be plausible mistakes (e.g., common calculation errors, sign errors, unit confusion)
- `explanation` field: 2-4 sentences explaining the solution step-by-step
- Use `optionExplanations` on at least 40% of questions (the ones where wrong answers represent specific misconceptions)
- IDs: `mst-001` through `mst-NNN`
- Tags: include topic tags like `['arithmetic', 'fractions']`, `['algebra', 'equations']`, etc.
- Difficulty spread: ~30% easy, ~50% medium, ~20% hard

**MST Lessons (src/data/lessons/mst-lessons.ts):**
Author `mstLessons: SectionLesson` with 4 topics matching question distribution:
1. Arithmetic Fundamentals (3-4 concept cards): fractions operations, decimal-percent-fraction conversion, order of operations, estimation strategies
2. Algebra Essentials (3-4 cards): solving linear equations, translating word problems to equations, exponent rules, inequality basics
3. Geometry Foundations (3-4 cards): triangle properties + Pythagorean theorem, circle formulas, area/volume formulas, coordinate geometry basics
4. Problem-Solving Strategies (3 cards): rate-time-distance framework, percent change formula, work problems approach

Each card: heading, 2-3 sentence content, keyTakeaway (one-line summary). Leave svgDiagram as undefined for now (will be enhanced in Phase 3 or later).
  </action>
  <verify>
    <automated>cd "C:/Users/grant/My Drive/Claude Projects/astb-flight-trainer" && npx tsc --noEmit && node -e "import('./src/data/questions/mst.ts').catch(() => console.log('Module check skipped - expected in TS project'))" && echo "Types valid"</automated>
  </verify>
  <done>mstQuestions array has 100+ items, all typed correctly as MultipleChoiceQuestion. mstLessons has 4 topics with 3-4 concept cards each. TypeScript compiles clean.</done>
</task>

<task type="auto">
  <name>Task 2: RCT question bank and lessons</name>
  <files>src/data/questions/rct.ts, src/data/lessons/rct-lessons.ts</files>
  <action>
**RCT Question Bank (src/data/questions/rct.ts):**
Author 50+ questions using the PassageQuestion type. RCT is passage-based: each PassageQuestion has a reading passage + 2-4 sub-questions.

**Structure: ~15-18 passages, each with 2-4 sub-questions, totaling 50+ sub-questions.**

**Passage topics (ASTB-appropriate -- military/aviation/science/history themes preferred):**
- Military history/operations (~4 passages): e.g., history of naval aviation, aircraft carrier operations, military strategy concepts
- Science/technology (~4 passages): e.g., aerodynamics principles, radar technology, weather systems, materials science
- Current affairs/policy (~3 passages): e.g., defense policy, international relations, environmental regulations
- General knowledge (~4 passages): e.g., leadership theory, human factors in aviation, navigation concepts

**Sub-question types per passage:**
- Main idea: "What is the primary purpose of this passage?"
- Detail/inference: "According to the passage, what can be inferred about..."
- Vocabulary in context: "The word 'X' as used in line Y most nearly means..."
- Author's purpose/tone: "The author's attitude toward X can best be described as..."

**Quality standards:**
- Passages: 150-250 words each, clear prose, ASTB-appropriate reading level
- Each sub-question has distinct correct answer derivable from the passage
- `explanation` on the PassageQuestion: general summary of passage themes
- Each sub-question has its own `explanation` explaining why the answer is correct
- Use `optionExplanations` on at least 30% of sub-questions
- IDs: `rct-001` through `rct-NNN` (one ID per passage)
- Tags: include topic + question type tags like `['military-history', 'main-idea']`
- Difficulty spread: ~30% easy, ~50% medium, ~20% hard

**RCT Lessons (src/data/lessons/rct-lessons.ts):**
Author `rctLessons: SectionLesson` with 3 topics:
1. Active Reading Strategies (3-4 cards): identifying main idea, reading for purpose, note structure/transitions, managing time on passages
2. Question Type Strategies (3-4 cards): main idea questions approach, inference vs stated detail, vocabulary in context technique, author purpose/tone cues
3. Common Traps (3 cards): extreme answer choices, "not stated" vs "implied", misreading negatives in questions

Each card: heading, 2-3 sentence content, keyTakeaway.
  </action>
  <verify>
    <automated>cd "C:/Users/grant/My Drive/Claude Projects/astb-flight-trainer" && npx tsc --noEmit && echo "Types valid"</automated>
  </verify>
  <done>rctQuestions array has 15+ passages with 50+ total sub-questions, all typed as PassageQuestion. rctLessons has 3 topics with concept cards. TypeScript compiles clean.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes -- all questions match their type definitions
2. MST has 100+ questions with IDs mst-001 through mst-NNN
3. RCT has 50+ sub-questions across 15+ passages with IDs rct-001 through rct-NNN
4. Both lesson files export valid SectionLesson objects
5. `npm run build` succeeds (code-splitting for question files works)
</verification>

<success_criteria>
- MST question bank: 100+ questions, 4 topic areas, difficulty spread, explanations on all
- RCT question bank: 50+ sub-questions across 15+ passages, multiple question types
- MST lessons: 4 topics, 3-4 cards each
- RCT lessons: 3 topics, 3-4 cards each
- All content is ASTB-appropriate difficulty and thematically relevant
- TypeScript compiles clean
</success_criteria>

<output>
After completion, create `.planning/phases/02-quiz-engine-and-content/02-02-SUMMARY.md`
</output>
